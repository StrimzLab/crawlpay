# Suppress deprecation noise from pnpm internals
node-options=--no-deprecation

# Strict peer deps — catch resolution mistakes early
strict-peer-dependencies=true

# Don't auto-install peers — force explicit declaration
auto-install-peers=false

# Save exact versions for reproducible builds
save-exact=false
save-prefix=^
auto-install-peers=true
