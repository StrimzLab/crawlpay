{
  "name": "crawlpay",
  "version": "0.0.1",
  "private": true,
  "description": "Open pay-per-crawl infrastructure on Arc + Nanopayments",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "demo": "tsx scripts/demo-crawl-session.ts",
    "seed": "tsx scripts/seed-publishers.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.7.0",
    "@types/node": "^20.11.0",
    "prettier": "^3.2.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}